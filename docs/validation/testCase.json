{
   $jsonSchema: {
      required: [ "name", "description", "config", "topology", "tests", "BOM", "testPlanId" ],
      properties: {
         name: {
            bsonType: "string",
            description: "must be a string and is required"
         },
         description: {
            bsonType: "string",
            description: "must be a string and is required"
         },
         config: {
            bsonType: "string",
            description: "must be a string and is required"
         },
         topology: {
            bsonType: "string",
            description: "must be a string and is required"
         },
         tests: {
            bsonType: "array",
            description: "must be an array and is required"
         },
         BOM: {
            bsonType: "array",
            description: "must be an array and is required"
         },
         testPlanId: {
            bsonType: "objectId",
            description: "must be an objectId and is required"
         },
      }
   }
 }